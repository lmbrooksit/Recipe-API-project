services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "80:8080"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
